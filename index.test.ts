import { IntelliHr } from "./index";

describe("intelli-hr-typescript-sdk", () => {
    it("initialize client", async () => {
        const intellihr = new IntelliHr({
            apiKey: "API_KEY",
        });
    });
});
